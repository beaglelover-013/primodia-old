# 普利莫迪亚前端全面伪0层改造 TODO

用途：这份文件是后续施工清单。每完成一项就勾选，避免只修单点问题、忘记同类问题在其他界面也存在。

状态说明：

- [ ] 未开始
- [/] 进行中
- [x] 已完成

## 0. 总原则

- [x] 建立本 TODO 文件。
- [x] 全项目禁止把 AI 当规则裁判：金钱、库存、地点、商铺、种植、酒窖、做菜、上菜、设施、人员分配等结果必须以前端结算为准。
- [x] 全项目禁止依赖 AI 的 MVU/JSONPatch 作为主权威；AI 输出只允许作为叙述、候选命名、隐藏数据补充。
- [/] 全项目禁止用“穷举关键词”判断场景；统一从当前权威位置、动作类型、当前页面上下文、存档快照判断。已移除商铺自然输入发送前的“离店/转店”关键词猜测，并限制 `<shop>` 只在本回合明确生成商铺时才可接管商铺快照。
- [x] 所有玩家行动都必须有“上一状态 -> 本回合动作 -> 前端结算 -> AI叙述 -> 存档快照”的承接链。

## 1. 项目结构收口

- [/] 建立/整理 `types/`：统一 GameSave、GameAction、ActionResult、ActionLog、LocationSnapshot、ShopSnapshot、InventoryItem 等类型。
- [/] 建立/整理 `engine/`：把规则裁判从页面和 store 中拆出。
- [/] 建立/整理 `services/`：变量读写、聊天楼层、AI生成、世界书路由、提示词调试都走适配层。
- [x] 已创建第一批结构入口：`types/domain.ts`、`engine/location.ts`、`engine/time.ts`、`services/save.ts`。
- [x] store 已开始使用统一地点标准化、商铺地点判断、时间推进基础计算和存档常量。
- [/] 保留 `stores/` 只做状态读取、派发动作、刷新 UI。当前核心写入已收进 Action / store 方法，剩余是后续把大型 store 拆到更多 engine/service 文件的结构性整理。
- [/] 页面组件只发出玩家意图，不直接改库存、地点、金钱、设施等权威数据；街坊商铺页已移除固定商铺池和固定商品池，酒馆改名也已收进 store 写入快照。

## 2. 主存档与变量权威

- [x] 明确 chat 变量为主存档，MVU/楼层文本只作为回放和兼容层。
- [x] 主存档写入统一入口：读写都走同一个 save service。
- [x] 主存档包含 `schemaVersion`、`savedAt`、`lastTickAt`、`currentMessageId`、`branchBaseMessageId`。
- [x] 主存档包含世界位置：区域、地点、具体位置、当前场景类型、当前商铺 id/名称、在场角色。
- [x] 主存档包含库存：食材、调料、成品、酒水、杂物，且所有增减都为增量更新，不整段替换库房。
- [x] 主存档包含临时商铺快照：店名、店主、描述、货架、购买篮子、来源楼层。
- [x] 主存档包含烹饪/饮品/酱料占位结果和待 AI 命名结果。
- [x] 主存档包含读档快照，读档时必须恢复正文、商铺、库存、地点、行动日志和页面状态；已新增按楼层保存的快照表，读档优先恢复对应楼层快照。
- [/] 添加 zod/schema 校验与迁移，旧结构读取时自动补缺字段。当前已加入主存档读取补缺保护和可见迁移状态，后续如要更严可再换成完整 zod schema。

## 3. 统一 Action 入口

- [x] 所有按钮、选项、自定义输入都必须进入统一 `dispatchAction`。
- [x] `dispatchAction` 已改为通过 `engine/dispatcher.ts` 的统一派发器进入各动作 resolver。
- [x] 每个 Action 必须先 `tickSave`，再校验，再结算，再写快照。
- [x] ActionResult 固定返回：成功/失败、显示颜色、结算摘要、是否需要 AI、库存变化、金钱变化、地点变化、页面建议。派发器会自动补全钱袋、库存、地点和页面建议。
- [/] 清理页面内直接修改状态的逻辑，改成派发 Action。当前剩余主要是纯 UI 状态（当前页、选中角色、正文流式开关等），权威数据已收进 Action/store 方法。
- [x] 增加“玩家自然语言行动”通用 Action：不穷举“回/去/离开”等词，而是把文本作为当前场景行动处理。
- [x] 增加“当前场景继续交流”通用 Action：编年录输入框不固定酒馆，必须使用当前权威地点。

## 4. Tick 时间推进

- [/] `tickSave` 只由 engine 负责，不由 AI 判断经过多久；当前已抽出 `engine/time.ts` 作为时间计算入口，后续可继续把各 resolver 内的主动快进也收束进 engine 层。
- [x] 加离线结算上限，避免长时间离线造成世界爆炸式推进。
- [/] 时间推进统一影响：时间段、种植、发酵、营业快进已接入；精力、设施等待、商铺自然刷新仍需进一步细化。
- [x] UI 时间显示统一：日期 + 时间段，小字/tooltip 显示具体时间。
- [x] 所有 Action 日志记录具体时段与钟点；ActionResult 现在会自动携带本次前后时间变化，并写入给 AI 的标准结算单。

## 5. 地点与场景系统

- [/] 建立通用 LocationSnapshot：区域、地点、具体位置、场景类型、场景来源、关联对象 id。
- [x] 顶部当前位置只读主存档，不读旧文本残留。
- [x] 页面显示以当前位置判断：不在商铺就不显示临时商铺货架。
- [x] 玩家在任意页面输入行动时，都从当前 LocationSnapshot 承接，不默认从酒馆出发。
- [x] 离开商铺、回酒馆、去厨房、去街坊、去其他店铺等都使用通用地点变化，不写死按钮流程；商铺自然输入不再发送前强行改地点。
- [x] 删除或弱化“返回街坊”强制按钮逻辑；玩家可自然输入离开，前端根据权威地点更新界面。
- [/] 读档必须恢复当时地点和页面状态，而不是只恢复正文；已优先读取楼层快照，并使用统一场景类型切回商铺/农田/库房/编年录。

## 6. 商铺闭环

- [x] FIND_SHOP：只确认玩家在当前区域寻找店铺，不创建长期地点。
- [x] VISIT_SHOP：前端创建临时商铺快照，AI只补店名、店主、氛围、货架内容。
- [x] `<shop>` 解析支持旧行格式、普通 JSON 格式和嵌套店铺 JSON 格式，并兼容“货架/今日货架/商品列表/服务列表”等同义字段。
- [x] `<shop>` 解析支持商品备注/描述/说明/介绍/详情/效果/用途，并在卡片上显示。
- [x] 商铺是否显示只看主存档当前位置和当前商铺快照，不看旧楼层里是否有 `<shop>`，且已移除固定店铺池/固定商品池，不能再伪造当前商铺。
- [x] BUY_ITEMS：前端扣钱、扣货架余量、加入库房，AI不能再反算。
- [x] 购买后不会清空全部货币，只按总价扣除。
- [x] 购买后不会整段替换库房，只增量加入/合并对应物品；生成请求会携带完整前端权威库房快照，AI 局部 JSONPatch 不作为主权威。
- [x] 和店主交流不离开商铺，仍保留当前商铺快照。
- [x] 离开商铺时根据生成后的权威地点/MVU清除当前商铺快照或标记为非当前，避免下回合误以为还在买东西。
- [x] 读档到商铺楼层时恢复商铺快照；读档到商铺后续交流楼层时也能恢复当时商铺快照。已修正“楼层无变量快照时误用当前残留地点恢复旧商铺”的问题，并关闭旧固定店池复活货架的后门。

## 7. 库房与物品

- [/] 统一物品结构：名称、数量、分类、价格铜币、标签、来源；变量快照不再输出库房备注，前端内部仍保留 `desc` 只用于商品/成品展示。
- [x] 成品/酒水的搭配判定使用：灾难级、严重冲突、轻微冲突、无冲突、经典搭配、绝佳搭配、奇迹。
- [x] 普通食材/调料/杂物不强制品质标签。
- [x] 入库分类规则统一：AI给分类则按分类；缺失时进入杂物，允许前端手动改分类。
- [x] 增加“移动分类/整理物品”Action，专门处理杂物归类，不靠拖拽误操作直接改库存。
- [x] 所有库存扣减不能让数量变负；数量为0时统一清理。
- [x] 所有库存变更写入 Action 日志，便于回查是谁改的；购买、做菜、上菜、收获、开桶、手动整理都已走 Action 日志。

## 8. 做菜、饮品、酱料、上菜

- [x] COOK_DISH：前端扣材料并创建待命名成品占位。
- [x] AI只按菜品生成引擎输出叙述与 `<craft_result>`，不得重新扣材料。
- [x] `<craft_result>` 只用于把占位成品改名/补标签/补价格，不新增第二份库存；若编号不完全匹配，回退到最近待命名占位。
- [x] 做饮品、做酱料和做菜分开技法池，但共用同一制作 Action 框架。
- [/] 发酵/桶装类结果进入酒窖桶队列；即食结果进入成品或酒水。已有基础入桶逻辑，仍需后续补成熟天数和桶容量规则。
- [x] SERVE_DISH：前端按价格和数量收钱、扣成品，AI只叙述上菜反应。
- [x] 做菜次数由前端统计并推进厨艺等级。
- [x] 做菜、上菜、做饮品、做酱都必须能从同一个“本次选择篮子”读取物品。

## 9. 农田与酒窖

- [x] 种子作为杂物/种子类物品，种植时由前端扣除并进入田地队列。
- [x] 每次收获生成批次食材，同批次共享 1-2 个标签。
- [x] 收获后自动同步入库房，不靠 AI 写变量。
- [x] 酒桶/酱桶开桶后自动同步入成品/酒水/调料或继续桶装，不靠 AI 写变量。当前开桶统一进入酒水；发酵制作结果可进入桶队列。
- [x] 灌装完成后对应桶内容应减少或清空。
- [x] 扩建/删除田地和桶位走 Action，支持撤销或日志回查。当前支持田畦扩建/删除与酒桶开桶清除，桶位删除后续可再细化为单独按钮。

## 10. 酒馆区域与设施

- [x] 区域设施添加走 Action：前端扣钱、加设施、写区域快照。
- [x] “确认建造并登记入册”类文案改为更像玩家行动的发送语句。
- [x] 员工分配走 Action，只记录名字和区域，不穷举职位。
- [x] 客房区支持多个房间，每个房间设施独立。
- [x] 区域平面图只展示当前存档设施和人员，不要求 AI 判断准确方位。
- [x] 酒馆名称允许玩家修改并写入主存档。

## 11. 人物与主角

- [x] 主角单独页面，配角只放人物羁绊页。
- [x] 主角烹饪等级由前端做菜次数推进。
- [x] 配角生命、精力、膀胱等数值显示数字和阶段文字。
- [x] 配角 CG 链接不作为核心变量；作为前端可维护图床链接集合。
- [x] 人物互动 Action 使用当前地点和在场角色，不默认酒馆。

## 12. 地图、大世界与商路/水路

- [x] 地图坐标按游戏距离重排，保留缩放/拖拽。
- [x] 城市点选显示预览，不必启程才能查看。
- [x] 当前地点框和城市预览框分离。
- [x] 商路作为第二图层：翡叶香路、铁砧隘路、绢稻之路、沙金之路、铁砧海路。
- [x] 水路作为可选图层：银冠河、旅人河、翠江、沼龙江、翡河。
- [x] 旅行 Action 由前端计算距离、载具、时间，不靠 AI。
- [x] 世界书路由扫描词由当前地点/区域/商路/水路生成，并作为独立 `should_scan` 注入，不再拿整段行动提示词扫描世界书。

## 13. AI 提示词与上下文承接

- [x] 提示词必须包含最近剧情承接，不让 AI 只看本回合动作。
- [x] 特殊动作提示词不能覆盖上下文；只能补充本回合格式要求。
- [x] 统一“权威局势”写法，避免重复出现互相矛盾的系统段落；生成接口已改为“世界书扫描词 + 权威局势叙述段”两段注入，不再把完整内部提示词重复当成玩家输入。
- [x] 禁止 AI 输出可见 `<NARRATIVE>`、`<CONTEXT_conception>`、`<Analysis>`。
- [x] 隐藏块必须在 `<maintext>` 后，前端隐藏显示。
- [/] 如果 AI 未输出 `<maintext>`，前端要有容错提取正文，但不能显示隐藏块；做菜仅有 `<craft_result>` 时已有兜底，普通叙事仍要求 maintext。

## 14. 楼层、读档、回档与分支

- [x] 每次 AI 回复后保存本楼层快照：正文、选项、sum、地点、库存、商铺、页面状态。
- [/] 读档不仅显示旧正文，也恢复旧快照到前端；已有楼层快照则恢复完整前端状态，没有快照时仍走旧 MVU 兼容。
- [/] 读档后不立即覆盖后续楼层；只有发送新行动时才创建分支/覆盖后续；继续发送前会优先恢复读档楼层快照，再删除后续楼层。
- [x] 如果环境没有楼层删除接口，必须提示真实限制，并避免多个前端同时渲染。当前删除接口缺失或删除失败时会写入日志并拒绝继续发送，避免读档后接到最新楼层。
- [/] 读档后发送新行动，楼层号必须从读档点继续，而不是接着最新楼层；已接入楼层删除接口和快照恢复，仍需在酒馆实机确认环境是否允许删除。
- [x] 阅读模式、读档模式、当前最新模式明确区分。正文页会标出“本回合正文/临时读档”，读档状态有独立提示条和回到最新楼层按钮。

## 15. AI 回复接回前端

- [x] AI叙述是消息，不是权威状态；日志中 AI 叙事标记为非权威。
- [x] 前端日志区区分：玩家指令、系统结算、AI叙述、告警、规则。
- [x] AI隐藏块解析后只能走允许的接收器：商铺、制作结果、选项、总结；非商铺生成动作里的 `<shop>` 会被忽略。
- [x] 不允许 AI 的 JSONPatch 整段替换主存档；已有前端权威快照时，MVU/JSONPatch 不回灌覆盖资源、库存与人员等主存档。
- [x] 流式显示开关只影响正文显示体验，不影响最终存档。

## 16. 调试与自检面板

- [x] 调试面板显示当前主存档 JSON。
- [x] 支持导出/导入主存档。
- [x] 显示 schema 版本与迁移状态。当前主存档 JSON 内含 schemaVersion，设置页会显示读取时是否补齐旧结构字段。
- [x] 显示当前世界书扫描文本。
- [x] 显示本次 AI 注入内容/最终提示词预览。
- [x] 显示最近 Action 日志。
- [x] 健康检查：货币非负、库存非负、地点存在、商铺状态一致、当前商铺货架、制作占位、库存连续性、队列时间合理、读档快照完整。
- [x] 一键执行只读自检，不修改存档。

## 17. UI 与宽屏适配

- [x] 宽屏下正文区域可滚动，不被底部输入栏挡住。
- [x] 宽屏下人物、地图、商铺、库房等侧栏都能滚动。
- [x] 商铺商品卡显示名称、价格、数量、标签、描述/备注。
- [x] 滚动条统一中世纪账本风格。
- [x] 顶部货币显示五种货币颜色。
- [x] 选项列表、输入框、按钮状态保持一致。生成中会禁用正文选项、自定义输入、读档条目、底部草稿和发送按钮。

## 18. 全局回归测试

- [x] 构建通过。
- [x] 首页能读取最新 `<maintext>`。
- [x] `<option>` 最多显示四项，支持字母和数字。
- [x] `<sum>` 用于读档摘要。
- [x] `<shop>` 旧格式和 JSON 格式都能捕捉。
- [/] 访问商铺 -> 交流 -> 购买 -> 离开 -> 去新地点，地点和上下文连续。代码层已改为权威地点 + 楼层快照 + 最近剧情承接，并加入只读自检；仍需酒馆实机跑完整链路确认。
- [/] 买东西不会清空货币，不会清空旧库存。代码层已改为按总价扣钱、增量入库、阻止 AI 整段覆盖，并加入库存骤降自检；仍需酒馆实机确认。
- [/] 做菜不会生成双份成品。代码层已改为先建占位、`<craft_result>` 回填占位，并加入待命名占位自检；仍需酒馆实机确认不同模型输出。
- [/] 读档后商铺、正文、变量、楼层都一致。代码层已接入楼层快照恢复、后续楼层删除保护和快照完整性自检；仍需酒馆实机确认删除接口可用。
- [/] 刷新页面后能从 chat 主存档继续。代码层启动时优先读取 chat 主存档并自动补缺字段；仍需酒馆实机刷新确认。
